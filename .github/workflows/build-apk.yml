name: Build Android APK

on:
  push:
    branches: [ main ]
  workflow_dispatch: {}

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
          cache-dependency-path: Android/package-lock.json

      - name: Install npm dependencies
        working-directory: ./Android
        run: npm ci

      - name: Copy web assets into Android project
        shell: bash
        run: |
          mkdir -p Android/android/app/src/main/assets/public
          cp -R www/. Android/android/app/src/main/assets/public/

      - name: Verify Android Gradle project
        shell: bash
        run: |
          test -f Android/android/settings.gradle
          test -f Android/android/gradlew
          test -f Android/android/app/build.gradle
          test -f Android/android/app/capacitor.build.gradle
          echo "Android project verified"

      - name: Grant execute permission for gradlew
        run: chmod +x Android/android/gradlew

      - name: Build debug APK
        working-directory: ./Android/android
        run: ./gradlew assembleDebug --stacktrace

      - name: Verify APK
        shell: bash
        run: test -f Android/android/app/build/outputs/apk/debug/app-debug.apk

      - name: Upload APK as build artifact
        uses: actions/upload-artifact@v4
        with:
          name: actionables-debug-apk
          path: Android/android/app/build/outputs/apk/debug/app-debug.apk